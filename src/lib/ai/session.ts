/**
 * Client-side session key the intake page uses to hand its computed
 * RecommendationResult (see src/lib/ai/recommend.ts) off to the
 * recommendation page. This is a placeholder for real server-side session
 * storage — see ARCHITECTURE.md for the persistence plan.
 */
export const INTAKE_STORAGE_KEY = "htww-intake-result";
